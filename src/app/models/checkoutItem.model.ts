export class CheckoutItemModel { // newBasketItem
  constructor(
      public name: string,
      public price: number,
      public name: string,
      public sum: number,
      public count: number,
  ) {
  }
}